import React from 'react';

export default function Test() {
  return (
    <div style={{ background: '#000', color: '#fff', padding: '2rem', textAlign: 'center', minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <div>
        <h1>✅ React is working!</h1>
        <p>If you see this, the app is loading correctly.</p>
      </div>
    </div>
  );
}
